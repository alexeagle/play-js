# Disabling pnpm [hoisting](https://pnpm.io/npmrc#hoist) by setting `hoist=false` is recommended on
# projects using rules_js so that pnpm outside of Bazel lays out a node_modules tree similar to what
# rules_js lays out under Bazel (without a hidden node_modules/.pnpm/node_modules)
hoist=false
# Workarounds for vite
public-hoist-pattern[]=rollup
public-hoist-pattern[]=esbuild
public-hoist-pattern[]=picomatch
public-hoist-pattern[]=fdir
public-hoist-pattern[]=tinyglobby
# Workarounds for lit
public-hoist-pattern[]=lit-element
public-hoist-pattern[]=lit-html
public-hoist-pattern[]=@lit/reactive-element
